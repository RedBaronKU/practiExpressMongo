<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ind_Comments</title>
</head>
<body>
    <ul>
        <%=comment.id%>
        <h3><%=comment.username%> :</h3> <b> <%=comment.comment %> </b></li>
    </ul>
    <a href="/comments">Back to Home</a>
    <form action="/comments/<%=comment.id%>?_method=DELETE" method="post">
        <button>Click to Delete</button>
    </form>
    <form action="/updateComment/<%=comment.id%>">
        <button>Click to Edit</button>
    </form>
</body>
</html>